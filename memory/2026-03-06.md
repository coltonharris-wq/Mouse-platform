# 2026-03-06 — Daily Log

## 00:04 — Free Hours Fix (Initial Attempt)
- **Issue:** New signups weren't getting 2 free work hours despite onboard page saying "2 free work hours credited"
- **Initial fix attempt:** Added `work_hours_balance: 2` to the customer insert in signup API (`f7da58c9`)
- **Second fix:** WorkHoursContext was fetching from `/api/work-hours` which used `SUPABASE_SERVICE_KEY` env var, but Vercel only has `SUPABASE_SERVICE_ROLE_KEY`. Fixed to fall back to correct var. Commit `75d0c245`.

## 00:25 — The REAL Root Cause Found
- **Problem:** After the env var fix, Colton reported "worked for 2 seconds then said need more hours"
- **Investigation:** Queried Supabase `customers` table — only 3 old test records. 10+ auth users from tonight with ZERO matching customer records.
- **Root cause #1:** Signup route inserted columns that DON'T EXIST on `customers` table: `first_name`, `last_name`, `account_type`. Supabase returned error but code used try/catch (Supabase JS doesn't throw, returns `{data, error}`). Every insert silently failed.
- **Root cause #2:** `company_name` column has NOT NULL constraint — signup sent `null` when empty.
- **Root cause #3:** Customer ID format — existing records use `cst_` prefix, signup was sending raw UUIDs.
- **Fix:** Rewrote signup insert to only use columns that exist: `id` (cst_ prefix), `email`, `company_name`, `status`, `work_hours_balance`, `work_hours_purchased`, `created_at`, `updated_at`. Now returns `customerId` to frontend.
- **Commit:** `e51750c2` — deployed via `vercel --prod`
- **Result:** ✅ Colton confirmed 2.0 hours showing correctly on portal

## 00:35–01:00 — King Mouse Chat 401/400 Fix (3 stacked bugs)
- **Bug 1:** Wrong Moonshot base URL — code used `api.moonshot.cn`, key works on `api.moonshot.ai`
- **Bug 2:** Wrong model name — `kimi-k2-0711` doesn't exist, correct is `kimi-k2.5`
- **Bug 3:** `kimi-k2.5` is a reasoning model, rejects `temperature: 0.7` (only temp=1 allowed)
- **Files fixed:** `app/api/chat/route.ts`, `app/api/vm/chat/route.ts`, `lib/llm-provider.ts`
- **New Moonshot API key:** `sk-fNNLY6aNRewMuDTKqvoIUPLcPyNJ9VEbDYNRuhgKdDjiT9SP` (named "mouse customer api key" in Moonshot dashboard)
- **Old key was dead:** `sk-mrls9bQceHL59emlGtK8FNoIHjkFh3RLJKE1LyaDDqEwo06v` — returns 401 on all endpoints
- **Vercel env updated** + deployed via `vercel --prod`
- **Commits:** `1c0c90ae`, `2fb9cb02`
- **Result:** ✅ Chat works for basic messages. ❌ First message after page load still returns 400 (intermittent)

## 01:13 — Cloud Mode Chat is NOT OpenClaw
- **Problem:** King Mouse cloud mode is just a raw Kimi API call with a static system prompt. No memory, no tools, no SOUL.md, no Owner Logic Engine. It's a chatbot, not OpenClaw.
- **Cloud mode system prompt:** Static `KING_MOUSE_BASE` in `lib/king-mouse-context.ts` with Supabase user context injected. Conversation history loaded from `conversations` table (last 20 msgs).
- **What it lacks vs real OpenClaw:** persistent memory files, tool access, learning, sub-agents, workspace

## 01:20 — COLTON'S DECISION: Every Customer Gets a Real OpenClaw VM

### Architecture Decision
- Every customer signup → auto-provision Orgo VM in shared workspace
- OpenClaw installed automatically on VM, configured from signup answers
- King Mouse IS OpenClaw — full agent pipeline (SOUL.md, memory, tools)
- Employees = sub-agents on King Mouse's VM (not separate VMs)
- Dashboard shows chat to King Mouse VM; VM visibility can come later (next week)
- Cloud mode fallback stays as temporary safety net but real product = VM

### NEXT STEPS (V14 Sprint — Priority Order)

1. **Auto-provision VM on signup**
   - After customer record created + payment confirmed → trigger Orgo VM creation
   - Use existing `ORGO_API_KEY` + `ORGO_WORKSPACE_ID` from env
   - Store `computer_id` in `employee_vms` table tied to customer
   - VM status tracking: provisioning → running → active

2. **Auto-install OpenClaw on VM**
   - Use existing Mouse OS runtime tarball (496MB, provisions in ~60s per V12 notes)
   - Configure from signup answers (business name, industry, etc.)
   - Generate SOUL.md, USER.md, MEMORY.md for the customer's King Mouse
   - Set up gateway with Kimi K2.5 as model, web chat channel
   - Reference: V12 commit `8acd772d`, V13 all 6 items done

3. **Wire dashboard chat → VM chat bridge**
   - `/api/vm/chat` route already exists and works
   - Just needs: customer has active VM → chat goes through `chat-bridge.mjs` on VM
   - Already tested E2E (commit `d2ab6a81`)

4. **Employees = sub-agents (NOT separate VMs)**
   - King Mouse uses OpenClaw sub-agent system for employees
   - No new VMs per employee — all run on King Mouse's VM
   - This is cheaper and simpler than V11's approach

5. **Customer-facing E2E test**
   - Signup → VM provisions → chat works → employee hire works
   - Must pass before tomorrow's demos

### What's Already Done (leverage this)
- Orgo VM integration (V11)
- Mouse OS runtime tarball (V12) — 60s provision
- King Mouse as full OpenClaw instance (V13)
- E2E tests passing (hire → provision → chat)
- Chat bridge (`chat-bridge.mjs`) working
- Ed25519 auth, reference templates, non-fatal skill install

### Key Files
- VM chat route: `app/api/vm/chat/route.ts`
- Cloud fallback: `app/api/chat/route.ts`
- LLM provider: `lib/llm-provider.ts`
- Context builder: `lib/king-mouse-context.ts`
- Orgo lib: `lib/orgo.ts`
- Signup: check `app/api/auth/signup/route.ts` or similar

### Env Vars (Vercel)
- `ORGO_API_KEY=<redacted - see Vercel>`
- `ORGO_WORKSPACE_ID=<redacted - see Vercel>`
- `MOONSHOT_API_KEY=<redacted - see Vercel>`
- Moonshot base URL: `https://api.moonshot.ai/v1`
- Model: `kimi-k2.5` (reasoning model, no custom temperature)

## Actual Supabase `customers` Table Schema
```
id, reseller_id, company_name, email, plan_tier, stripe_customer_id, 
stripe_subscription_id, stripe_subscription_status, status, 
created_at, updated_at, work_hours_balance, work_hours_purchased
```

## Key State (End of Session)
- **Live:** https://mouse-platform-demo.vercel.app
- **Repo:** github.com/coltonharris-wq/Mouse-platform (main branch)
- **Commits today:** `f7da58c9`, `b3c710b7`, `75d0c245`, `e51750c2`, `1c0c90ae`, `2fb9cb02`
- **Signup flow:** ✅ Working
- **Work hours display:** ✅ Working
- **King Mouse chat (cloud mode):** ✅ Working (but it's just a chatbot, not OpenClaw)
- **King Mouse chat (VM mode):** ❌ No VMs provisioned for customers yet
- **NEXT:** Wire auto-VM-provision on signup → install OpenClaw → make it the real product

## 01:27 — New Session: Colton Pushes for Overnight Autonomous Build

### Colton's Ask
1. **MUST HAVE:** OpenClaw on VM and working upon account creation — non-negotiable
2. **Signup answers → OpenClaw config:** Use interview/onboard answers to configure the customer's OpenClaw instance
3. **Auto-compaction at 80k tokens:** Wants Mouse to self-manage context without human intervention
4. **Auto new session:** Wants Mouse to auto-reset sessions when tokens get high so Colton doesn't have to click /new while sleeping

### Analysis of Current State
- `mouse-os-provision.ts` (836 lines) already handles: tarball download, OpenClaw install, config generation from `interviewAnswers`, USER.md generation, gateway launch
- `kickOffProvision()` works within Vercel 10s timeout
- `lib/orgo.ts` has `createComputer()`, `waitForReady()`, `executeBash()`
- **The gap:** signup route creates customer but never creates Orgo VM or triggers provisioning — ~30-40 lines of code to wire

### Compaction / Auto-Session Findings
- Config already has `compaction.mode: "safeguard"` + `memoryFlush.softThresholdTokens: 150000`
- OpenClaw compacts automatically but does NOT auto-create new sessions
- For overnight autonomous work: **cron jobs with isolated sessions** = fresh 200k tokens per run
- Sub-agents (coding agents) also get own token budgets

### Proposed Plan
- **Option A (recommended):** Spawn Claude Code sub-agent to wire signup → VM flow now
- **Option B:** Cron-chained work for longer autonomous runs
- Awaiting Colton's go-ahead to launch

## 02:04 — Screenshots Confirm the Bugs

Colton sent 4 screenshots showing the actual user experience:

### Screenshot 1-2: King Mouse Chat
- Shows "AI error: 400" on first message (intermittent Moonshot API issue)
- King Mouse responds: "I don't have access to a long-running memory file yet — that's coming soon"
- **This is CLOUD MODE** — the fallback `/api/chat` route, NOT the VM chat
- **Proof no VM was created** — if a VM existed, it would route to `/api/vm/chat`

### Screenshot 3: Work Hours Dashboard
- Shows 2.0 hours available, usage stats look correct
- This part is working

### Screenshot 4: Usage History Table (THE SMOKING GUN)
- Table columns: DATE, SERVICE, **VENDOR COST**, HOURS CHARGED, **MARGIN**
- Example: "Chat (Kimi) | $0.0014 | -1 min | 30x"
- **This confirms the UI bug** — customers can see our internal costs and margins

### Root Cause Confirmed
The onboard page sends `user?.userId` (raw UUID) to marketplace/hire, but the customers table uses `cst_` prefixed IDs. So `checkBalance()` fails silently, no VM is created, and the error is swallowed by the "graceful degradation" catch block. User sees "success" but gets cloud mode instead of a real VM.

**Fixes needed:**
1. Change `user?.userId` to `user?.customerId` in onboard page
2. Fix error handling to show real errors, not fake success
3. Hide VENDOR COST and MARGIN columns from work hours page
4. Verify Supabase schemas
5. Test E2E

### Session Model Switched to Kimi K2.5
- Opus session ended (budget constraints: $21 remaining, $500 spent in 2 days)
- Kimi K2.5 now active for overnight work
- Orchestration doc: `memory/overnight-task.md`
- Progress tracker: `memory/overnight-progress.md`
- AI Landscape cron re-enabled (every 6 hours)
