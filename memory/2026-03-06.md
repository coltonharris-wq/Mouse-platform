# 2026-03-06 — Daily Log

## 00:04 — Free Hours Fix (Initial Attempt)
- **Issue:** New signups weren't getting 2 free work hours despite onboard page saying "2 free work hours credited"
- **Initial fix attempt:** Added `work_hours_balance: 2` to the customer insert in signup API (`f7da58c9`)
- **Second fix:** WorkHoursContext was fetching from `/api/work-hours` which used `SUPABASE_SERVICE_KEY` env var, but Vercel only has `SUPABASE_SERVICE_ROLE_KEY`. Fixed to fall back to correct var. Commit `75d0c245`.

## 00:25 — The REAL Root Cause Found
- **Problem:** After the env var fix, Colton reported "worked for 2 seconds then said need more hours"
- **Investigation:** Queried Supabase `customers` table — only 3 old test records. 10+ auth users from tonight with ZERO matching customer records.
- **Root cause #1:** Signup route inserted columns that DON'T EXIST on `customers` table: `first_name`, `last_name`, `account_type`. Supabase returned error but code used try/catch (Supabase JS doesn't throw, returns `{data, error}`). Every insert silently failed.
- **Root cause #2:** `company_name` column has NOT NULL constraint — signup sent `null` when empty.
- **Root cause #3:** Customer ID format — existing records use `cst_` prefix, signup was sending raw UUIDs.
- **Fix:** Rewrote signup insert to only use columns that exist: `id` (cst_ prefix), `email`, `company_name`, `status`, `work_hours_balance`, `work_hours_purchased`, `created_at`, `updated_at`. Now returns `customerId` to frontend.
- **Commit:** `e51750c2` — deployed via `vercel --prod`
- **Result:** ✅ Colton confirmed 2.0 hours showing correctly on portal

## 00:35 — Next Bug: "AI error: 401" on King Mouse Chat
- **Screenshot:** Colton sent message "hey, so you can save memory about me and stuff?" → got "AI error: 401"
- **This is the King Mouse chat page** (`/portal/king-mouse` or similar)
- **Likely cause:** The chat endpoint is failing auth — could be:
  1. No King Mouse VM running for this new customer (the onboard hire may not have triggered, or VM isn't provisioned)
  2. The fallback cloud chat mode hitting an API that returns 401
  3. Moonshot API key not set on Vercel
- **Status:** NOT YET DEBUGGED — Colton starting new session for this

## Actual Supabase `customers` Table Schema
```
id, reseller_id, company_name, email, plan_tier, stripe_customer_id, 
stripe_subscription_id, stripe_subscription_status, status, 
created_at, updated_at, work_hours_balance, work_hours_purchased
```
No: first_name, last_name, account_type (these are in auth user_metadata only)

## Key State
- **Live:** https://mouse-platform-demo.vercel.app
- **Commits today:** `f7da58c9`, `b3c710b7`, `75d0c245`, `e51750c2`
- **Signup flow:** ✅ Working (creates auth user + customer record with 2 hrs)
- **Work hours display:** ✅ Working (portal + King Mouse page both show balance)
- **King Mouse chat:** ❌ Returns "AI error: 401" — NEXT TO FIX
- **Colton's test account:** newest signup, company "wefawef", email unknown (check most recent auth user)

## 00:39 — 401 Chat Bug: Root Cause Identified
- **Traced the full flow:**
  1. Frontend (`/dashboard/king-mouse/page.tsx`) calls `/api/vm/chat` first (Live VM mode)
  2. VM route returns `fallback: true` (no active VM for this test customer — no King Mouse hired)
  3. Falls back to `/api/chat` (Cloud Mode) with `userRole: 'customer'`
  4. Cloud mode routes customers to **Moonshot Kimi K2.5** (`kimi-k2-0711`)
  5. Kimi API at `api.moonshot.cn/v1/chat/completions` returns **HTTP 401**
- **Root cause:** `MOONSHOT_API_KEY` env var on Vercel is likely missing or the key (`sk-mrls9bQceHL59emlGtK8FNoIHjkFh3RLJKE1LyaDDqEwo06v`) is expired/invalid
- **Model routing logic:** Admin → Anthropic Opus, Customer/Reseller → Moonshot Kimi K2.5 (in `/api/chat/route.ts`)
- **Next step:** Verify the Moonshot key is valid (curl test), then ensure it's set in Vercel env vars
- **Status:** Waiting on Colton to confirm next action
