# 2026-03-07 — Mouse Platform Sprint

## Time
Saturday, March 7th, 2026 — 12:29 AM (EST)

## Decisions Made

### Migration Strategy (Partial)
- **012, 013, 014-part1** applied successfully
- **014-part2 (resellers table) BLOCKED** — existing table conflict
  - Existing `resellers` table has 4 rows with different schema (branding/config focus)
  - Migration expects reseller program schema (user_id, invite_code, commission_rate)
  - **Decision needed**: Rename old table or merge schemas?

### Gateway Dashboard
- Reconnected successfully (was showing "Offline" due to missing token)
- Token `test123` entered, dashboard now shows Health: OK

### E2E Test Infrastructure
- Added `ORGO_API_KEY` and `ORGO_WORKSPACE_ID` to local env
- Extended timeout to 15min for VM provisioning (was 20s, needs 5-10min)
- Previous E2E hit browser timeout at 35s during VM deploy

## State Changes

| Component | Before | After |
|-----------|--------|-------|
| Supabase migrations | 010 applied | 012, 013, 014-part1 applied |
| Gateway dashboard | Disconnected | Connected, Health: OK |
| Local env | Missing Orgo vars | ORGO_API_KEY, ORGO_WORKSPACE_ID set |
| E2E timeout | 20s | 15min |

## Technical Choices

### Migration 013 Adaptation
- Original migration tried to create new `payments` table
- Table already existed → adapted to `ADD COLUMN IF NOT EXISTS stripe_session_id`
- Created partial unique index: `WHERE stripe_session_id IS NOT NULL`

### Migration 014 Split
- Split into Part 1 (customers.user_id) and Part 2 (resellers table)
- Part 1 succeeded, Part 2 blocked by existing table

## Blockers

### Active
1. **Resellers table schema conflict**
   - Existing table: `id uuid, name, slug, email, logo_url, brand_primary...` (4 rows)
   - Migration expects: `id varchar, user_id, invite_code, commission_rate...`
   - **Owner**: Cursor/Colton decision needed

### Pending (from handoff)
2. Vercel cron: Enable `/api/cron/provision-retry` + CRON_SECRET
3. Re-run E2E test (full flow: signup → onboard → deploy → chat)
4. Manual QA: customer flow + reseller flow + /for-resellers
5. Deploy to production

## Key People/Projects

| Person/Project | Role | Context |
|----------------|------|---------|
| Cursor | Code/schema changes | Handed off migrations, fixing customer.id/usage alignment |
| Colton | Product decisions | Needs to decide on resellers table conflict |
| Mouse Platform | Main product | V14 sprint: auto-provision VM on signup, customer portal |
| Orgo | VM provisioning | E2E testing blocked on 5-10min provision time |

## Notes
- Gateway dashboard at http://localhost:18789 now functional
- E2E subagent running with extended timeout (15min)
- Signup page `router` fix deployed earlier today
- Auto-compaction + Doctor Visit button implemented in King Mouse chat
